services:
  - type: web
    name: gameframe-fastapi
    env: python
    region: ohio
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT