services:
  - type: web
    name: gameframe-js
    env: docker
    plan: starter
    autoDeploy: true
    disk:
      name: gameframe-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 10000
      - key: DB_PATH
        value: /var/data/app.db
