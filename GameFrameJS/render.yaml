services:
  - type: web
    name: gameframe
    env: docker
    plan: starter
    autoDeploy: true
    disk:
      name: gameframe-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 10000
      - key: DB_PATH
        value: /var/data/app.db
